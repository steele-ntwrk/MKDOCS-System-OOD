server {
  listen 8080;

  root {{ project_path }}/site;
  index index.html index.htm;
  try_files $uri $uri/ /index.html;

  server_name {{ server_name }};
  
  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ =404;
  }
}